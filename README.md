# dApp Coscience

/profile/:id
to see the profile of user
param

edge case  
register and wallet added, careful mapping
add require

abstract component for blockchain function buttons (pending => success)
